@model IEnumerable<WaslnyLib.Entity.Price>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<section class="hero-wrap hero-wrap-2 js-fullheight" style="background-image: url('../../Front/images/bg_3.jpg');" data-stellar-background-ratio="0.5">
    <div class="overlay"></div>
    <div class="container">
        <div class="row no-gutters slider-text js-fullheight align-items-end justify-content-start">
            <div class="col-md-9 ftco-animate pb-5">
                <p class="breadcrumbs"><span class="mr-2"><a href="index.html">Price<i class="ion-ios-arrow-forward"></i></a></span> <span>Index <i class="ion-ios-arrow-forward"></i></span></p>
                <h1 class="mb-3 bread">Price</h1>
            </div>
        </div>
    </div>
</section>




<div class="request-details">
    <div class="container">
        <nav class="pt-4" aria-label="breadcrumb">
            <ol class="breadcrumb breadcrumb-edit">
                <li class="breadcrumb-item"></li>
            </ol>
        </nav>

        <p>
            @Html.ActionLink("Create New", "Create")
        </p>
        <table class="table table-hover">
            <tr>
                <th>
                    <p style="margin-right: 20px;">Route To</p>
                </th>
                <th>
                    <p style="margin-right: 20px;">Car Classification Type</p>
                </th>
                <th>
                    <p style="margin-right: 20px;">Price</p>
                </th>
                <th>
                    <p style="margin-right: 20px;">Is Deleted</p>
                </th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Route.RouteTo)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CarClassification.CarClassificationType)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PriceMoney)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.IsDeleted)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.PriceID }) |
                        @Html.ActionLink("Details", "Details", new { id = item.PriceID }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.PriceID })
                    </td>
                </tr>
            }

        </table>
    </div>
</div>
